name: fileweightloss_gui
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.5
  process_run: ^1.2.2+1
  dotted_border: ^2.1.0
  desktop_drop: ^0.5.0
  cross_file: ^0.3.4+2
  cupertino_icons: ^1.0.8
  window_size:
    git:
      url: https://github.com/google/flutter-desktop-embedding
      path: plugins/window_size
  url_launcher: ^6.3.1

  media_kit: ^1.1.11
  media_kit_libs_audio: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
